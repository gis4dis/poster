{
  "name": "pster-app",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix modules/mc-client && npm run build --prefix modules/mc-client && npm run export --prefix modules/mc-client"
  }
}